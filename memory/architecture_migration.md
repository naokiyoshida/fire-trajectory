---
name: Architecture migration to Playwright + Node.js
description: 2026-05-10 決定。Tampermonkey + PowerShell + GAS から Playwright + Node + GAS に一本化する移行を進行中
type: project
---

2026-05-10、3層構成（Tampermonkey + PowerShell + GAS）から Playwright + Node.js + GAS に一本化することが決定された。一括移行（並行運用なし）。

**Why:** 既存3層構成は DOM 脆弱性、Chrome表示のちらつき、設定3箇所分散、テスト不可能、PC稼働必須など複数の弱点があった。ユーザーは「自動化重視・月次でOK・言語自由」を希望し、規約リスクと自動化の両立として Playwright によるローカル月次実行が最良と判断。

**How to apply:**
- 新コードは TypeScript + Playwright + googleapis (Sheets API) で書く
- 既存 `mf_sync_client.user.js` `RunSyncHidden.ps1` は Phase 7 で削除予定
- 既存 `gas_receiver_service.gs` は doPost / sync_data 系を削除し、`setupSimulation` と新規 `setupReports` のみ残す方向
- スケジュール: 月次1回（取引・資産まとめて）。バランス案ではなく最小頻度を選択
- 認証: Playwright storageState で Cookie 永続化。初回のみユーザーが手動ログイン (2FA含む)、以降ヘッドレス
- ローカル実行を起点とし、必要になればクラウド (Cloud Run / GitHub Actions) に同じコードで移行可能な設計とする
- 既存 GAS の Simulation シート構築ロジック（フィッシャー方程式、ライフイベント制御、月次360行）は温存

ナラティブ層に合わせる前提値（2026-05 家計入金モデルに刷新済み）:
- 基本生活費: 月35万
- 本人月収_家計入金: 月30万 / 本人ボーナス_年額_家計入金: 90万
- 配偶者月収_家計入金: 月13万 / 配偶者ボーナス_年額_家計入金: 0
- 本人年金: 月15万 (年180万) / 配偶者年金: 月6.5万 (年78万)
- 本人退職一時金: 300万 / 配偶者退職一時金: 0
- 想定利回り: 5%（名目）
- インフレ率: 2%（日銀目標 / 直近CPI 2.7〜3.2%。旧 0 から変更）

## 進捗 (2026-05-10 時点、commit 15d5ff2 で完了・push 済み)

完了:
- Phase 1-7 全コード実装、テスト 35/35 通過
- Sheets セットアップ (サービスアカウント認証、共有設定)
- Full Sync 実装と実機実行 (5,259 件取引 / 資産推移 1 件、2021/10〜2026/5)
- タスクスケジューラ登録 + StartWhenAvailable=true
- 全シート名・列名の日本語化と `quoteSheetName` 導入
- 「設定」シートに変動パラメータを切り出し (B17-B24):
  - 本人退職時一時金 / 配偶者退職時一時金 (本人/配偶者を分離)
  - 本人年金開始年齢 / 配偶者年金開始年齢 (繰上/繰下を独立に設定可)
  - FIRE射程の盤石/余裕閾値、シミュレーション終了年齢
- 表示形式 (`¥#,##0` / `yyyy/MM/dd`) と編集可能セルの色分け (黄=B列値セル(編集可), 水色=B列数式セル(編集非推奨,「現在の資産」), 灰=D列デフォルト, 濃青=セクション見出し, 2行目に凡例)
- メニュー「表示形式の適用」を追加（手動再適用用）
- 旧項目名 → 新項目名のマイグレーション処理 (退職時一時金 / 年金開始年齢 → 本人/配偶者2項目)

未完 (任意):
- メール通知 (Gmail アプリパスワード設定はユーザー任意)
- ねんきん定期便到着後の `本人/配偶者年金_年額` 値更新 (智絵さん 2026/06、本人 2027/03 に到達)

シート名と列名 (確定):
- 取引履歴 / 資産推移 / 手動入力資産 / 設定 / シミュレーション
- 月次収支 / カテゴリ別支出 / 純資産推移 / 資産配分 / FIRE射程
- 資産推移の列: 基準日/預金・現金/株式（現物）/株式（未上場）/投資信託/年金/ポイント/その他資産/資産総額/クレジット未払/住宅ローン/その他負債/負債総額/純資産/備考
- 手動入力資産: 項目/値/備考。受け付けるラベルは「未上場株式」(→stocks_unlisted) と「備考」(→notes) のみ

「設定」シート構造 (2026-05-16 家計入金モデル + セクション化に刷新):
- 1行目=ヘッダー / 2行目=凡例(A:D結合) / 3行目以降を **共通・本人(僕)・配偶者(妻)** の
  3セクションに分割。各セクション見出しは A:D 結合の濃青帯。C列に説明と入力例を表示。
- **行番号は固定でない**。`setupSimulation` の `layout` 配列でセクション/項目を上から定義し、
  実セル行は描画時に動的決定。
- 数式は行番号ではなく **項目名で INDEX/MATCH 参照**するヘルパー `dashLookup_(dashRef, key)`
  （= `INDEX(設定!$B:$B, MATCH("key", 設定!$A:$A, 0))`）を使う。
  setupSimulation の D マップも setupReportFireReadiness_ の参照も全て `dashLookup_` 経由。
  → 設定シートの並び替え・見出し挿入をしても数式が壊れない（旧「行番号同期が必要」問題は解消）。
- 項目キー一覧（家計入金モデル）: 現在の資産 / 基本生活費_月額 / ローン月額 / ローン完済予定日 /
  息子支援月額 / 息子支援終了日 / 運用利回り_名目 / インフレ率 / FIRE射程_盤石閾値 /
  FIRE射程_余裕閾値 / シミュレーション終了年齢 / 本人誕生日 / リタイア予定日 /
  本人月収_家計入金 / 本人ボーナス_年額_家計入金 / 本人退職時一時金 / 本人年金_年額 /
  本人年金開始年齢 / 配偶者誕生日 / 配偶者退職予定日 / 配偶者月収_家計入金 /
  配偶者ボーナス_年額_家計入金 / 配偶者退職時一時金 / 配偶者年金_年額 / 配偶者年金開始年齢
- 旧キー（本人手取り月収 / 配偶者年収_年額）は migration でサイレント破棄し新デフォルトで上書き。
  既存値の保持は項目名キーで突き合わせるため並び替えに非依存。
